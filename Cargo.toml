[package]
name = "system_stat"
version = "0.1.0"
edition = "2024"

[dependencies]
sysinfo = "0.39.0"
clap = { version = "4.0", features = ["derive"] }
crossterm = "0.29.0"

[profile.release]
codegen-units = 1
strip = true